server:
  port: ${PRODUCT_SERVICE_PORT:9001}

spring:
  application:
    name: product-service

  datasource:
    url: jdbc:postgresql://cs308-postgres.postgres.database.azure.com:5432/postgres?sslmode=require
    username: cs308user
    password: Sv9Und7nkd91
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info